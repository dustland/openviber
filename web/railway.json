{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'ERROR: Deploy from repo root, not web/. Set Root Directory to empty in Railway settings.' && exit 1"
  },
  "deploy": {
    "startCommand": "node build/index.js"
  }
}
