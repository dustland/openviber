# Web App Environment Variables
# Copy this to web/.env and fill in values

# Public app URL used in OAuth callbacks
APP_URL=http://localhost:6006

# Supabase project URL and anon key for authentication
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Optional: service role key for server-side user profile upserts
SUPABASE_SERVICE_ROLE_KEY=

# OpenRouter API key — the platform's built-in AI provider. Used as the default
# for all users who haven't configured their own provider keys in Settings.
# Users' own keys are stored in the database (user_settings.ai_providers).
OPENROUTER_API_KEY=

# Google OAuth — Required for Gmail integration (Settings > Integrations)
# Create credentials at https://console.cloud.google.com/apis/credentials
# Add redirect URIs: http://localhost:6006/auth/google/callback and https://yourdomain.com/auth/google/callback
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=

# Encryption key for secrets stored in DB (OAuth tokens, environment secrets)
# If not set, a default dev key is used. Set a strong random value in production.
# OPENVIBER_ENV_SECRET_KEY=

# E2E test mode — set to "true" to bypass OAuth and onboarding for AI agents.
# When active, unauthenticated requests to protected routes receive a synthetic
# test user automatically. This lets Cursor, Codex, Playwright, etc. exercise
# the full UI without real GitHub credentials. Ignored in production (NODE_ENV=production).
# E2E_TEST_MODE=true

# Dev only: pseudo node for local daemon (no onboarding). Set to the same value
# your local daemon sends as X-Viber-Id (e.g. in ~/.openviber/vibers/default.yaml or --viber-id).
# When set, GET /api/nodes will upsert a viber_nodes row for the current user so the local node appears as a normal node.
# OPENVIBER_DEV_NODE_ID=local
# OPENVIBER_DEV_NODE_NAME=Local Dev
