# Web App Environment Variables
# Copy this to web/.env and fill in values

# Public app URL used in OAuth callbacks
APP_URL=http://localhost:6006

# Supabase project URL and anon key for authentication
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Optional: service role key for server-side user profile upserts
SUPABASE_SERVICE_ROLE_KEY=

# Dev only: pseudo node for local daemon (no onboarding). Set to the same value
# your local daemon sends as X-Viber-Id (e.g. in ~/.openviber/vibers/default.yaml or --viber-id).
# When set, GET /api/nodes will upsert a viber_nodes row for the current user so the local node appears as a normal node.
# OPENVIBER_DEV_NODE_ID=local
# OPENVIBER_DEV_NODE_NAME=Local Dev
